# The real configuration lives outside the web root on the server and is never
# committed — it holds the API key and the password hash.
config.php

# Local testing only.
.local/
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.log
.DS_Store

# dist/ is deliberately NOT ignored: DEPLOY.md asks for those zips and the
# person deploying has no way to build them.
